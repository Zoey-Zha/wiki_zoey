spring:
  datasource:
    url: jdbc:mysql://rm-8vb1e746ez02l37260o.mysql.zhangbei.rds.aliyuncs.com/wikidev?characterEncoding=UTF8&autoReconnect=true&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: wikidev
    password: 1qaz!QAZ
    driver-class-name: com.mysql.cj.jdbc.Driver
# 配置Mybatis
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml

# 配置后端端口号，怀疑跟前端的8080冲突
server:
  port: 8090

# 增加数据库查询结果日志打印，打印所有sql日志，参数，结果
logging.level.com.zoey.mapper:
  trace

